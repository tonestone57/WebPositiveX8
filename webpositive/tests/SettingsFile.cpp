#include "SettingsFile.h"

status_t OpenSettingsFile(BFile& file, const char* fileName, uint32 mode) {
    return B_ERROR;
}

status_t GetSettingsPath(BPath& path, const char* fileName) {
    return B_ERROR;
}
